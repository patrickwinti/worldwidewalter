app.https-redirect.enabled=false
# Emit the OpenAPI document with deterministically ordered keys so the committed
# openapi.json (see OpenApiSpecTest) has a stable, diff-friendly representation.
springdoc.writer-with-order-by-keys=true
